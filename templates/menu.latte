{parameters \Crell\MiDy\PageTreeDB2\Folder $folder, int $depth = 2}
{varType \Crell\MiDy\PageTreeDB2\PageTree $pageTree}

<ul n:if="$depth > 0" n:inner-foreach="$folder as $page">
    <li>
        <a href="{$page->path}">{$page->title}</a>
        {if $page instanceof \Crell\MiDy\PageTreeDB2\Folder}
            {include menu.latte, folder: $page, depth: $depth - 1}
        {/if}
    </li>
</ul>
